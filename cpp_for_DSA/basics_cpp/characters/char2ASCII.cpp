#include <iostream>
using namespace std;
int main() {
    int num;
    cout<<"enter a num to find its ASCII val:",
    cin>>num;
    cout<<"ASCII value of given number is : "<< (char)num;
    //converting chracter into num .ex: 65 is num so ASCII is A
}